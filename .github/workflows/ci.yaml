name: ci-wf
on:
  pull_request:
    branches:
      - develop
  push:
    branches:
      - develop
  jobs:
    linting-job:
      runs-on : ubuntu-latest
      steps:
        - name: step of lintinng
          run: echo "allah always help me"
    buid-job:
      needs: "linting-job"
      runs-on: ubuntu-latest
      steps:
        - name: Step1 of build jobs
          run : echo "step1 of build job"
      
